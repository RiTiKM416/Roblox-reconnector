# Roblox Termux Reconnector

This script connects Rooted Android emulators directly to a specific Roblox Game ID, automatically restarting it if it crashes.

## 🚀 Easy Install via Termux
Open **Termux** on your device and paste this single command to download and run it instantly:

```bash
curl -sL https://raw.githubusercontent.com/RiTiKM416/Roblox-reconnector/main/install.sh | bash
```

**Features:**
- Real-time connection timer
- Auto-reconnects on game closure / crash
- Easy background tracking
- Stop/Resume functionality

*Note: Root access mapping required (Grant su permissions when prompted).*
